{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build:server": "tsc -p tsconfig.json",
    "build:web": "tsc -p tsconfig.lusco.json && cp -R renderer/* dist/web/",
    "build": "npm run build:server && npm run build:web",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "watch:web": "tsc -p tsconfig.lusco.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
